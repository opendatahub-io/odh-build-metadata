Grid operator installed.

  Release:   {{ .Release.Name }}
  Namespace: {{ .Release.Namespace }}
  Image:     {{ include "grid-operator.image" . }}

Verify the operator is running:

  kubectl get deployment {{ include "grid-operator.fullname" . }} -n {{ .Release.Namespace }}
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "grid-operator.name" . }},app.kubernetes.io/instance={{ .Release.Name }}

Check CRDs:

  kubectl get crd gridnetworks.grid.praxis-proxy.io
  kubectl get crd gridsites.grid.praxis-proxy.io
  kubectl get crd inferenceproviders.grid.praxis-proxy.io
{{- if .Values.metrics.service.enabled }}

Run Helm tests:

  helm test {{ .Release.Name }} -n {{ .Release.Namespace }}
{{- end }}

Next steps:

  1. Create TLS and SWIM encryption Secrets.
  2. Apply a GridNetwork to join a mesh.
  3. Apply InferenceProviders to register backends.
  4. Install a compatible Praxis gateway separately.

CRD lifecycle:

  Helm installs CRDs from the crds/ directory on initial install, but does
  NOT upgrade or delete them on helm upgrade or helm uninstall. To upgrade
  CRDs from a release artifact:

    helm pull oci://ghcr.io/praxis-proxy/charts/grid-operator --version <version> --untar
    kubectl apply -f grid-operator/crds/

  Or from the source repository:

    kubectl apply -f https://raw.githubusercontent.com/praxis-proxy/grid/<version>/deploy/crds/gridnetwork.yaml
    kubectl apply -f https://raw.githubusercontent.com/praxis-proxy/grid/<version>/deploy/crds/gridsite.yaml
    kubectl apply -f https://raw.githubusercontent.com/praxis-proxy/grid/<version>/deploy/crds/inferenceprovider.yaml

  From a local checkout:

    kubectl apply -f charts/grid-operator/crds/

  To remove CRDs (this deletes ALL custom resources of those types):

    kubectl delete crd gridnetworks.grid.praxis-proxy.io
    kubectl delete crd gridsites.grid.praxis-proxy.io
    kubectl delete crd inferenceproviders.grid.praxis-proxy.io

Chart documentation: https://github.com/praxis-proxy/grid/tree/main/charts/grid-operator
{{- if .Values.resourceNamespaces }}

Resource access granted in additional namespaces:
{{- range .Values.resourceNamespaces }}
  - {{ . }}
{{- end }}
{{- end }}
